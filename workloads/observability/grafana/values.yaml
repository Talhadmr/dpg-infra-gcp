replicas: 1

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

persistence:
  enabled: true
  size: 5Gi
  accessModes: ["ReadWriteOnce"]

service:
  type: ClusterIP
  port: 80

adminPassword: "admin"
